SELECT
	  COUNT(PARTICIPANTS.SYS_GUID)	   
FROM PARTICIPANTS
INNER JOIN GRADES ON PARTICIPANTS.SYS_GUIDFK = GRADES.SYS_GUID
INNER JOIN EGEEXTPARTICIPANTS ON EGEEXTPARTICIPANTS.SYS_GUIDFK = PARTICIPANTS.SYS_GUID
WHERE EGEEXTPARTICIPANTS.ELEVENRESULTEND IN ('8')